import { DisplayHrsPipe } from './display-hrs-pipe';

describe('DisplayHrsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayHrsPipe();
    expect(pipe).toBeTruthy();
  });
});
