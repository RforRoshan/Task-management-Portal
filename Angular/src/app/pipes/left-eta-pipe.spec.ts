import { LeftETAPipe } from './left-eta-pipe';

describe('LeftETAPipe', () => {
  it('create an instance', () => {
    const pipe = new LeftETAPipe();
    expect(pipe).toBeTruthy();
  });
});
